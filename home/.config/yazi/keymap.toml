[mgr]
prepend_keymap = [
  # drag from Yazi
  { on = "<C-d>", run = "shell -- ripdrag %s --all" },

  # quickshell
  { on = "!", run = 'shell "$SHELL" --block', desc = "Open $SHELL in CWD" },

  # fastjump
  { on = "f", run = "plugin jump-to-char", desc = "Jump to char" },

  # lazy
  { on = "L", run = "shell --block -- lazygit", desc = "LazyGit CWD" },

  # tabs
  { on = "<C-n>", run = "tab_switch --relative 1", desc = "Switch to next tab" },
  { on = "<C-p>", run = "tab_switch --relative -1", desc = "Switch to previous tab" },
  { on = ["t", "c"], run = "close", desc = "Close current tab" },

  # bookmarks jump
  { on = ["g", "b"], run = "back", desc = "Go back" },
  { on = ["g", "C"], run = "cd ~/Code", desc = "Go code" },
  { on = ["g", "e"], run = "cd sftp://edelgard//deep_end", desc = "Go Edelard" },
  { on = ["g", "i"], run = "cd ~/Sync/Inbox", desc = "Go inbox" },
  { on = ["g", "n"], run = "cd ~/Code/nixos-skwrls", desc = "Go nix" },
  { on = ["g", "o"], run = "cd ~/.homesick/repos/dotfiles", desc = "Go dotfiles" },
  { on = ["g", "p"], run = "cd sftp://piper", desc = "Go Piper" },
  { on = ["g", "s"], run = "cd ~/Pictures/Screenshots", desc = "Go screenshots" },

  # add fzf and zoxide
  { on = ["g", "z"], run = "plugin zoxide", desc = "Jump via zoxide" },
  { on = ["g", "Z"], run = "plugin fzf", desc = "Jump via fzf" },

  # mount local disks (mount/unmount/eject/jump inside the picker)
  { on = ["M", "m"], run = "plugin mount", desc = "Mount manager" },

  # ftp vfs (mp3 player); prompts for host[:port]
  { on = ["M", "f"], run = "plugin ftp", desc = "Connect to FTP host" },
]
