#!/usr/bin/env bash
# Pop a fuzzel text box and send the typed line to the Obsidian inbox.
text=$(fuzzel --dmenu --prompt-only="Inbox: " </dev/null) || exit 0
[[ -n $text ]] || exit 0
exec ~/bin/inbox "$text"
