[user]
  name = David Aronsohn
  email = WagThatTail@Me.com
[core]
	excludesfile = /Users/dos/.gitignore_global
  editor = vim
[push]
  default = current
[alias]
  prgithub = "!f() { git fetch origin pull/$1/head:pr-$1 && git checkout pr-$1;  }; f"
  prstash = "!f() { git fetch $1 refs/pull-requests/$2/from:$3;  } ; f"
  co = checkout
  ci = commit
  br = branch
  st = status
[diff]
  tool = Kaleidoscope
  compactionHeuristic = true
[merge]
  tool = Kaleidoscope
[difftool]
  prompt = false
[mergetool]
  prompt = false

#Silly tools fight for config down here
[hub]
  protocol = ssh
[filter "lfs"]
  clean = git-lfs clean %f
  smudge = git-lfs smudge %f
  required = true
[mergetool "Kaleidoscope"]
  cmd = ksdiff --merge --output \"$MERGED\" --base \"$BASE\" -- \"$LOCAL\" --snapshot \"$REMOTE\" --snapshot
  trustexitcode = true
[difftool "Kaleidoscope"]
  cmd = ksdiff --partial-changeset --relative-path \"$MERGED\" -- \"$LOCAL\" \"$REMOTE\"
[interactive]
  # diffFilter = diff-highlight
[difftool "sourcetree"]
	cmd = opendiff \"$LOCAL\" \"$REMOTE\"
	path = 
[mergetool "sourcetree"]
	cmd = /Applications/SourceTree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
	trustExitCode = true
